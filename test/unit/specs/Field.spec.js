// import Vue from 'vue';
// import Field from '@/components/Field';

describe('Field.vue', () => {
  it('should render remove button', () => {
    // I have no idea what I'm doing.

    return true;
    /* const Constructor = Vue.extend(Field);
    const vm = new Constructor().$mount();
    expect(vm.$el.querySelector('header .remove'))
      .to.exist(); */
  });
});
